name: Test Workflow
on: push
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: "Get the code"
        uses: actions/checkout    
      - name: Install / Nodejs 
        uses: actions/setup-node     
      - name: dependency install
        run: npm install
      - name: Test
        run: npm test

