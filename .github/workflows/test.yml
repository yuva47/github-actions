name: Test Workflow
on: push
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: "Get the code"
        uses: actions/checkout@v4    
      - name: Install / Nodejs
        uses: actions/setup-node@v4
      - name: Install Dependency
        run: npm install
      - name: Test
        run: npm test

